version: "3.9"
services:
  mailer:
    build:
      context: .
      dockerfile: mail-service/Dockerfile
      args:
        SERVICE_NAME: mail-service
    container_name: mailer-app
    restart: unless-stopped
    environment:
      # App settings
      APP_PORT:            "${MAILER_PORT}"
      # Mailer
      SMTP_USER:           "${SMTP_USER}"
      SMTP_PASS:           "${SMTP_PASS}"
      SMTP_HOST:           "${SMTP_HOST}"
      SMTP_PORT:           "${SMTP_PORT}"
    ports:
        - "${MAILER_PORT}:${MAILER_PORT}"
    networks:
      - app-net

  weather:
    build:
      context: .
      dockerfile: weather-subscription-service/Dockerfile
      args:
        SERVICE_NAME: weather-service
    container_name: weather-app
    restart: unless-stopped
    environment:
      # App settings
      APP_PORT:            "${WEATHER_PORT}"
      READ_TIMEOUT:        "${READ_TIMEOUT}"
      WRITE_TIMEOUT:       "${WRITE_TIMEOUT}"
      IDLE_TIMEOUT:        "${IDLE_TIMEOUT}"
      GIN_MODE:            "release"

      GRPC_ADDR: "mailer:${MAILER_PORT}"

      # Database connection
      DB_HOST:             "postgres"
      DB_PORT:             "${DB_PORT}"
      DB_USER:             "${DB_USER}"
      DB_PASSWORD:         "${DB_PASSWORD}"
      DB_NAME:             "${DB_NAME}"
      DB_SSL_MODE:         "${DB_SSL_MODE}"
      MAX_OPEN_CONNS:      "${MAX_OPEN_CONNS}"
      DB_MAX_IDLE_CONNS:   "${DB_MAX_IDLE_CONNS}"
      DB_MAX_IDLE_TIME:    "${DB_MAX_IDLE_TIME}"

      # Migrations
      MIGRATION_PATH:      "${MIGRATION_PATH}"

      # Weather API
      WEATHER_API_KEY:     "${WEATHER_API_KEY}"
      WEATHER_SERVICE_URL: "${WEATHER_SERVICE_URL}"

      # Redis
      REDIS_ADDR:          "redis:6379"
      REDIS_DB:            "${REDIS_DB}"
      REDIS_PASSWORD:      "${REDIS_PASSWORD}"
    ports:
      - "${WEATHER_PORT}:${WEATHER_PORT}"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - app-net

  postgres:
    image: postgres:15-alpine
    container_name: weather-db
    restart: unless-stopped
    environment:
      POSTGRES_USER:  ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB:     ${DB_NAME}
    ports:
      - "${DB_PORT}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - app-net

  redis:
    image: redis:7-alpine
    container_name: weather-redis
    restart: unless-stopped
    command: ["redis-server", "--requirepass", "${REDIS_PASSWORD}"]
    ports:
      - "6378:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - app-net

networks:
  app-net:
    driver: bridge

volumes:
  postgres_data:
  redis_data: